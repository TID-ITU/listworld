import { useEffect, useState } from 'react';
import './ProductList.css'

const List = () => {
    // const [list, setList] = useState([])

    // useEffect(() => {
    //     if (list.length === 0) {

    //     }
    // },)
    return (
        <ul>
            
        </ul>
    )
}

export default List;